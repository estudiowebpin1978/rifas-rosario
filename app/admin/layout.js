export const metadata = {
  title: 'Admin Panel - Eco Rifas',
  description: 'Panel de administración de rifas, pagos y usuarios.',
}

export default function AdminLayout({ children }) {
  return children
}
